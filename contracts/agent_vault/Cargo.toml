[package]
name = "agent-vault"
version = "1.0.0"
edition = "2021"
description = "AgentVault — On-chain registry for autonomous AI agents on Casper"

[dependencies]
casper-contract = "1.4.4"
casper-types = "1.5.0"

[lib]
crate-type = ["cdylib", "lib"]

[profile.release]
lto = true
opt-level = "z"
strip = true
